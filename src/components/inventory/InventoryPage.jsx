'use client';

import InventoryPageInner from '@/app/AdminUser/inventory/page';

export default function InventoryPage() {
  // Re-export the existing Admin InventoryPage UI/logic as a shared component
  return <InventoryPageInner />;
}
